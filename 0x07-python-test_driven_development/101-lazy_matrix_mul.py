#!/usr/bin/python3

"""Defines a matrix multiplication function using Numpy"""

import numpy as np

def lazy_matrix_mul(m_u, m_b):
    """Returns the multiplication of two matrices

    Args:
        m_a: The first matrix
        m_a: The second matrix

    """

    return (np.matmul(m_a, m_b))
